任意のホストへのポートフォワーディングを維持するサービス

- ポートフォワード先は設定ファイルで定義する。1つの設定ファイルに複数接続先を定義可能 (リモートホスト, リポートポート, ローカルポート, ユーザー名)
- ssh接続認証は、公開鍵のみの前提で良い
- ポートフォワーディングの死活監視を定期的に自動で行い、切れたら再接続する
- Linux (RedHat系), Mac どちらでも動作する bash スクリプトで実現すること
- 依存コマンドは、各OSに標準インストールされているものか、可能な限りdnfまたはbrewでインストールできるものをつかう。 
- 接続状況を一覧表示できるような機能
- Linuxでは systemd service として登録できるようにすることも考慮

